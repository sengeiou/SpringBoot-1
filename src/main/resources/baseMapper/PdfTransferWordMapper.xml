<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.onejane.multisource.mapper.basemapper.PdfTransferWordMapper">
    <resultMap id="BaseResultMap" type="com.onejane.multisource.entity.PdfTransferWordEntity">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="pdf_url" property="pdfUrl" jdbcType="VARCHAR"/>
        <result column="word_url" property="wordUrl" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findUrl" parameterType="com.onejane.ali.oss.vo.TransferQueryVo" resultMap="BaseResultMap">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        *
        from pdf_transfer_word t
        where t.id=#{id}
    </select>

    <insert id="insertPdf" parameterType="com.onejane.multisource.entity.PdfTransferWordEntity" useGeneratedKeys="true"
            keyProperty="id">
        insert into
            pdf_transfer_word (id,
                               pdf_url,
                               word_url)
        values (#{id},
                #{pdfUrl},
                #{wordUrl})
    </insert>

    <update id="updateWord" parameterType="com.onejane.multisource.entity.PdfTransferWordEntity">
        update pdf_transfer_word t
        set
        word_url = #{wordUrl}
        <trim prefix="WHERE" prefixOverrides="AND | OR ">
            <if test="id != null and id!='' ">
                and t.id=#{id}
            </if>
        </trim>
    </update>

</mapper>